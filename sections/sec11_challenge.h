#ifndef _SEC11_CHALLENGE_H_
#define _SEC11_CHALLENGE_H_

#include <vector>

void print_numbers(const std::vector<int> &numbers_list, size_t list_size);

void add_number(std::vector<int> &numbers_list);

void get_mean(const std::vector<int> &numbers_list, size_t list_size);

void get_smallest(const std::vector<int> &numbers_list, size_t list_size);

void get_largest(const std::vector<int> &numbers_list, size_t list_size);

void section_11_challenge();

#endif // _SEC11_CHALLENGE_H_